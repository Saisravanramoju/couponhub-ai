# Copy to .env. Never commit .env or real credentials.
# Use a long random password without semicolons (connection string delimiter).
POSTGRES_PASSWORD=
# Optional. Configure a Chat Completions model supporting JSON mode.
OPENAI_API_KEY=
OPENAI_MODEL=
